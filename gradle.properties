# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.jdk.cache.disabled=true
# Disabling the JDK image transform can help if jlink is failing in the current environment
android.ext.enableJdkImage=false
